import { styled } from "~/stitches.config";
const ButtonStyled = styled("button", {
  appearance: "none",
  cursor: "pointer",
  py: "5px",
  px: "1rem",
  bg: "$violet10",
  borderRadius: "1rem",
});

export { ButtonStyled };
